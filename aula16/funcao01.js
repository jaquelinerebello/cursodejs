function parimpar (n) {
    if ( n%2 == 0 ) {
        return 'Par!'
    } else {
        return 'ímpar!'
    }
}

/* Dessa forma mostrei o resultado na tela:
let res = parimpar(4)
console.log(res)
*/

// Posso fazer dessa forma também, pedi pra escrever e pedia  função com parâmetro:

console.log(parimpar(223))